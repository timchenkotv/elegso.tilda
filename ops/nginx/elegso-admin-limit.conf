# VM 240 receives all public requests from the TLS edge, so per-client IP
# limiting here would treat every visitor as one address. This conservative
# global ceiling still prevents a fast bcrypt exhaustion attack without
# interfering with normal interactive dashboard use.
limit_req_zone $server_name zone=elegso_admin_global:1m rate=5r/s;
