# Changelog — calc_nst / payments

## [v1.0.0] — draft (not tagged)
- Extracted from Tilda:
  - `v1/styles.css`, `v1/script.js` вынесены на GitHub.
  - `v1/template.html` + `v1/loader.js`, тонкий загрузчик в Tilda.
- Подключение в Tilda идёт с ветки `@main` (временное).
